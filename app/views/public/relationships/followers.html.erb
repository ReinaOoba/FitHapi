<div class="container">
  <h3><%= @user.name %>さんのフォロワー一覧</h3>
    <% if @users.exists? %>
      <%= render "public/relationships/follow_list", users: @users %>
    <% else %>
      <p>フォロワーはいません～！</p>

    <% end %>
</div>
