<div class="container">
  <h3><%= @user.name %>@<%= @user.account %>のフォロワー一覧</h3>
    <% if @users.exists? %>
      <%= render "admin/relationships/follow_list", users: @users %>
    <% else %>
      <p>フォロワーがいません</p>
    <% end %>
</div>