<div class="container">
  <div class="row">
    <div class="mini-title">
      <%= @user.name %> @ <%= @user.account %>さんの会員詳細
    </div>
  </div>
  <%= image_tag @user.get_profile_image(200,200), style: "border-radius: 50%" %>
  <div class="row">
    <div class="col-8 ml-5">
      <table class="table table-bordered ml-5">
        <tbody>
          <tr>
            <th class="col-4 text-center">ユーザーID</th>
            <td><%= @user.id %></td>
          </tr>
          <tr>
            <th class="col-4 text-center">ニックネーム</th>
            <td>
              <%= @user.name %>
            </td>
          </tr>
          <tr>
            <th class="col-4 text-center">アカウント名</th>
            <td>
              <%= @user.account%>
            </td>
          </tr>
          <tr>
            <th class="col-4 text-center">自己紹介</th>
            <td><%= @user.introduction %></td>
          </tr>
          <tr>
            <th class="col-4 text-center">メールアドレス</th>
            <td><%= @user.email %></td>
          </tr>
          <tr>
            <th class="col-4 text-center">会員ステータス</th>
            <td>
              <% if @user.active_for_authentication? %>
                <b class="text_success">有効</b>
              <% else %>
                <b class="text-muted">退会</b>
              <% end %>
            </td>
          </tr>
        </tbody>
      </table>
      <div class="mt-4 mb-4 text-center">
      <%= link_to "編集する", edit_admin_user_path(params[:id]), class: "btn btn-success mr-3" %>
    <!--%= link_to "投稿一覧", admin_user_articles_path(@user), class: "btn btn-primary" %>-->
    <!--%= link_to "まいトレ一覧", admin_mytrainings_path, class: "btn btn-primary" %>-->
      </div>
    </div>
  </div>
</div>
