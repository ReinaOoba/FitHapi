  <%= render "layouts/errors", obj: obj %>
  <%= form_with model: obj, url: @path, method: :post, local:true do |f| %>
    <div class="form-group row">
      <%= f.text_field :name, placeholder: "新しいワード", class: "form-control col-4" %>
      <%= f.submit "登録", class: "btn btn-success" %>
    </div>
  <% end %>